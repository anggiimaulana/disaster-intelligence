import type { PublicStats } from '@/types/public-disaster';

export const MOCK_STATS: PublicStats = {
  reportsToday: 128,
  activeWarnings: 12,
  highRiskZones: 3,
  citizenParticipation: 2458,
  isSimulation: true,
};
